# задаем исходный массив
arr = [1, 3, 5, 7, 10, 12, 15, 20]

# инициализируем счетчик и сумму
count = 0
summa = 0

# перебираем элементы массива
for elem in arr:
    if elem > 10:
        # если элемент больше 10 - увеличиваем счетчик и добавляем к сумме
        count += 1
        summa += elem

# вычисляем среднее арифметическое, если были найдены элементы больше 10
if count > 0:
    avg = summa / count
    print("Среднее арифметическое элементов массива, больших числа 10, равно:", avg)
else:
    print("В массиве нет элементов, больших числа 10")

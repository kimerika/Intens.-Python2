# Создаем массив с вещественными числами
numbers = [43.2, 55.1, 67.4, 49.8, 32.5, 52.6, 48.1, 59.3]

# Инициализируем переменную для хранения количества элементов, не больше 50,55 и кратных четырем
count = 0

# Проходимся по всем элементам массива с числами
for number in numbers:
    if number <= 50.55 and number % 4 == 0:
        count += 1

# Проверяем, что количество таких элементов равно длине массива
if count == len(numbers):
    print("Все элементы не больше 50.55 и кратны 4")
else:
    print("Не все элементы не больше 50.55 и кратны 4")


scores = [3, 5, 1, 2, 4] # пример списка очков команд

min_score_index = 0 # начинаем считать с первой команды
for i in range(1, len(scores)):
    if scores[i] < scores[min_score_index]:
        min_score_index = i

print(f"Наименьшее количество очков у команды #{min_score_index + 1}")

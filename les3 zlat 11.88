# задаем исходный массив
arr = [1, 3, 5, 7, 10, 12, 15, 20]

# задаем число, меньше которого будут находиться элементы массива
num = 10

# инициализируем счетчик и сумму
count = 0
summa = 0

# перебираем элементы массива
for elem in arr:
    if elem < num:
        # если элемент меньше заданного числа - увеличиваем счетчик и добавляем к сумме
        count += 1
        summa += elem

# вычисляем среднее арифметическое, если были найдены элементы меньше заданного числа
if count > 0:
    avg = summa / count
    print("Среднее арифметическое элементов массива, меньших", num, "равно:", avg)
else:
    print("В массиве нет элементов, меньших числа", num)

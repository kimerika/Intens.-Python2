
numbers = [5.7, 8.2, 6.9, 3.5, 10.1, 7.2, 9.6, 4.8, 5.4, 8.9]

# Проверка, превышает ли максимальный элемент минимальный не более чем на 25
max_num = max(numbers)
min_num = min(numbers)
if max_num - min_num <= 25:
    print("Максимальный элемент превышает минимальный не более чем на 25")

# Проверка, меньше ли минимальный элемент максимального более чем в 2 раза
if min_num * 2 < max_num:
    print("Минимальный элемент меньше максимального более чем в 2 раза")



# Создаем массив с числами
numbers = [7, 3, 9, 2, 8, 1, 6, 4, 5]

# Инициализируем переменную для хранения количества чисел, больших своих соседей
count = 0

# Проходимся по всем элементам массива, кроме первого и последнего
for i in range(1, len(numbers) - 1):
    if numbers[i] > numbers[i-1] and numbers[i] > numbers[i+1]:
        count += 1

# Выводим результаты
print("Число элементов, которые больше своих соседей:", count)


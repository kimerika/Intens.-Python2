
def is_triangle(a, b, c):
    """
    Функция принимает три длины сторон a, b, c в качестве параметров и возвращает True, если треугольник может быть построен,
    и False в противном случае.
    """
    if a <= 0 or b <= 0 or c <= 0:
        # Если хотя бы одна длина стороны меньше или равна нулю, то невозможно построить треугольник
        return False
    elif a >= b + c or b >= a + c or c >= a + b:
        # Если длина одной стороны больше или равна сумме двух других, то невозможно построить треугольник
        return False
    else:
        # Иначе треугольник может быть построен
        return True

# Пример использования функции

# Вводим длины трех сторон треугольника
a = float(input("Введите длину стороны a: "))
b = float(input("Введите длину стороны b: "))
c = float(input("Введите длину стороны c: "))

# Проверяем возможность построения треугольника и выводим результат
if is_triangle(a, b, c):
    print("Треугольник может быть построен")
else:
    print("Треугольник не может быть построен")


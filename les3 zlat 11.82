
# Создаем массив с числами
numbers = [9, 0, 0, 7, 6, 0, 1, 0, 0, 3, 0, 5]

# Инициализируем переменную для хранения количества пар соседних элементов, оканчивающихся на 0
count = 0

# Проходимся по всем элементам массива, кроме последнего
for i in range(len(numbers) - 1):
    if numbers[i] % 10 == 0 and numbers[i+1] % 10 == 0:
        count += 1

# Выводим результаты
print("Число пар соседних элементов, оканчивающихся на 0:", count)

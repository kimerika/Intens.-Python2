def sum_numbers():
    number = input("Введите число: ")
    if number == "":
        return 0.0
    else:
        return float(number) + sum_numbers()

result = sum_numbers()
print(f"Сумма чисел: {result}")
